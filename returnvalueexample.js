// We create a function that accepts two parameters.
// Integer values for rows and columsn
function monitorCount(rows, columns)
{
    return rows * columns;
}

const numOfMonitors = monitorCount(5, 4);
console.log(numOfMonitors);
